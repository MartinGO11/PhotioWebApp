import es from './es.json';
import en from './en.json';
import { items } from './esComponent';

export const dictionaryList = { es, en, items };

export const languageOptions = {
  // es: 'Español',
  items: 'Español',
  en: 'English'
};
